spring:
  application:
    name: customer
  datasource:
    url: jdbc:postgresql://postgres:5432/connect
    username: connect
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: true
    show-sql: true
