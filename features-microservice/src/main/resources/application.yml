server:
  port: 1111

spring:
  application:
    name: features

  profiles:
    active: 'postgres'

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true