server:
  port: 1111

spring:
  application:
    name: features

  profiles:
    active: 'postgres'

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true