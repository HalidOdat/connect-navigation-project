server:
  port: 2222

spring:
  application:
    name: routes

#  profiles:
#    active: 'postgres'

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
